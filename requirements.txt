# Streamlit Cloud requirements
# These mirror the main pyproject.toml dependencies but in pip format
# so Streamlit Cloud can install them without uv.

# Install the flight_predictor package itself from the repo root
-e .

streamlit>=1.35.0
joblib>=1.6.0
numpy>=1.26.0,<2.3.0
pandas>=2.2,<3
requests>=2.31.0
scikit-learn>=1.4.0
xgboost>=2.0.0
